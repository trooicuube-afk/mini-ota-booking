spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/miniota
    username: miniota
    password: miniota
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    show-sql: true
  thymeleaf:
    cache: false

miniota:
  jwt:
    secret: dev-only-miniota-jwt-secret-key-that-is-at-least-64-bytes-long-for-hmac-sha512!!

management:
  endpoint:
    health:
      show-details: always
      show-components: always
